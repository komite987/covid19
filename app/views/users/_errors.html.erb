<div class="card border-danger mb-3 col-lg-6 offset-lg-3 alert alert-success alert-dismissible fade show w-100" role="alert" >
    <div  class= "card-text text-danger">
        <h4>
            <%= pluralize(@errors.count, "error") %> found:
        </h4>
        <ul> <% @errors.each do |k , msg|%>
            <li><%= "#{k}" + " " + "#{msg}".gsub("[", "").gsub("]", "").gsub('"', "") %></li>
            <% end %>
        </ul>
    </div>
    <button type="button" class="btn close" data-dismiss="alert" aria-label="Close"> 
        <span aria-hidden="true">×</span> 
    </button> 
</div>
